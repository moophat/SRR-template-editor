name: Trigger Upstream Sync to Shared-Resources

on:
  push:
    branches: [main]
    paths:
      - 'template/**'

jobs:
  trigger-dispatch:
    runs-on: ubuntu-latest
    if: "!contains(github.event.head_commit.message, '[auto-sync]')"  # 🛡 loop prevention
    steps:
      - name: Dispatch reverse sync to shared-resources
        run: |
          curl -X POST \
            -H "Authorization: token ${{ secrets.SHARED_REPO_PAT }}" \
            -H "Accept: application/vnd.github+json" \
            https://api.github.com/repos/your-org/shared-resources/dispatches \
            -d '{
              "event_type": "push-upstream",
              "client_payload": {
                "consumer": "template-editor"
              }
            }'
